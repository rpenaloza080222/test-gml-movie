import { ImageUrlResolutionPipe } from './image-url-resolution.pipe';

describe('ImageUrlResolutionPipe', () => {
  it('create an instance', () => {
    const pipe = new ImageUrlResolutionPipe();
    expect(pipe).toBeTruthy();
  });
});
